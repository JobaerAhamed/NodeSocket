{
  "name": "chat",
  "version": "1.0.0",
  "description": "Epic chat app",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npm run build&&gh-pages -d build",
    "build": "npm run build",
    "eject": "npm run eject"
  },
  "author": "jobaerAhamed",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "socket.io": "^2.1.0"
  },
  "devDependencies": {
    "nodemon": "^1.17.3"
  }
}
